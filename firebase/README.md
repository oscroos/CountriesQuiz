# Firebase configuration files

Add the native Firebase config files here before building:

- `google-services.json` for Android
- `GoogleService-Info.plist` for iOS

Download both from Firebase Console > Project settings after adding the Android app
`com.countriesquiz.app` and the iOS app `com.countriesquiz.app`.
